import {
  OPEN_SOCKET,
} from '../actionTypes';
import io from 'socket.io-client';


export function openSocket() {
  return dispatch => {
  }
}




export function changeRoom(socket, newRoom) {
	return dispatch => {
	}
}
