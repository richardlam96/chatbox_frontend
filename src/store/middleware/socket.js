import io from 'socket.io-client';


const connectSocket = store => next => action => {
	console.log('Connecting to server socket');
	socket.
}
const socketChatboxListeners = store => next => action => {}
const socketFriendRequestListeners = store => next => action => {}

